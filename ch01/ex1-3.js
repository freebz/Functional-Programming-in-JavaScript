// 1.2.2 Pure functions and the problem with side effects

var counter = 0;
function increment() {
    return ++counter;
}



var showStudent = run(
    append('#student-info'),
    csv,
    find(db));

showStudent('444-44-4444');
